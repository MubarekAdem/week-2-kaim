pandas>=1.3
tqdm
# Transformers (optional, recommended for accuracy)
transformers>=4.30
torch>=1.12  # or install a version compatible with your platform (cpu/gpu)
# NLTK for fallback (vader)
nltk>=3.6
# If you will use VADER fallback, download resource: nltk.download('vader_lexicon')
